{
  "name": "novitas-monorepo",
  "private": true,
  "workspaces": [
    "web",
    "agent",
    "contract"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web"
  }
}
